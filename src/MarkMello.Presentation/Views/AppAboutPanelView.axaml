<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:MarkMello.Presentation.ViewModels"
             xmlns:views="using:MarkMello.Presentation.Views"
             mc:Ignorable="d"
             x:Class="MarkMello.Presentation.Views.AppAboutPanelView"
             x:DataType="vm:ShellViewModel">
      <Border
              Classes="mm-settings-panel mm-popover-left mm-app-about-panel"
              Width="320">
        <StackPanel>
          <Border Classes="mm-settings-head-shell">
            <Grid ColumnDefinitions="Auto,*,Auto"
                  ColumnSpacing="12">
              <Button Grid.Column="0"
                      Classes="mm-settings-close"
                      Command="{Binding ReturnToAppSettingsCommand}"
                      ToolTip.Tip="{Binding OverlayBackToSettings}">
                <views:LucideIcon Data="{DynamicResource LucideArrowLeftGeometry}"
                                  Width="14"
                                  Height="14" />
              </Button>
              <TextBlock Classes="mm-settings-title"
                         Grid.Column="1"
                         VerticalAlignment="Center"
                         Text="{Binding AboutHeader}" />
              <Button Grid.Column="2"
                      Classes="mm-settings-close"
                      Command="{Binding CloseOverlayCommand}"
                      ToolTip.Tip="{Binding OverlayCloseAbout}">
                <views:LucideIcon Data="{DynamicResource LucideXGeometry}"
                                  Width="14"
                                  Height="14" />
              </Button>
            </Grid>
          </Border>

          <Border Classes="mm-settings-body-shell">
            <StackPanel Spacing="0">
              <Border Classes="mm-setting-row">
                <Grid ColumnDefinitions="*,Auto"
                      ColumnSpacing="12">
                  <StackPanel Grid.Column="0"
                              Spacing="2">
                    <TextBlock Classes="mm-setting-label"
                               Text="{Binding AboutVersionLabel}" />
                    <TextBlock Classes="mm-setting-hint"
                               Text="{Binding AboutVersionHint}" />
                  </StackPanel>
                  <TextBlock Grid.Column="1"
                             Classes="mm-setting-meta"
                             Text="{Binding AboutVersion}" />
                </Grid>
              </Border>

              <Line Classes="mm-setting-divider"
                    StartPoint="0,0.5"
                    EndPoint="1,0.5" />

              <Border Classes="mm-setting-row">
                <Grid ColumnDefinitions="*,Auto"
                      ColumnSpacing="12">
                  <StackPanel Grid.Column="0"
                              Spacing="2">
                    <TextBlock Classes="mm-setting-label"
                               Text="{Binding AboutLicenseLabel}" />
                    <TextBlock Classes="mm-setting-hint"
                               Text="{Binding AboutLicenseHint}" />
                  </StackPanel>
                  <TextBlock Grid.Column="1"
                             Classes="mm-setting-meta"
                             Text="{Binding AboutLicense}" />
                </Grid>
              </Border>

              <Line Classes="mm-setting-divider"
                    StartPoint="0,0.5"
                    EndPoint="1,0.5" />

              <Border Classes="mm-setting-row">
                <StackPanel Spacing="8">
                  <TextBlock Classes="mm-setting-label"
                             Text="{Binding AboutCreditsLabel}" />
                  <WrapPanel Orientation="Horizontal"
                             ItemHeight="22">
                    <TextBlock Classes="mm-setting-body"
                               Text="{Binding AboutCreatedByPrefix}" />
                    <Button Classes="mm-inline-link"
                            Tag="https://ermolaev.tech"
                            Click="OnAboutLinkClick"
                            Content="Andrey Ermolaev" />
                    <TextBlock Classes="mm-setting-body"
                               Text="{Binding AboutCreditsPeriod}" />
                  </WrapPanel>
                </StackPanel>
              </Border>
            </StackPanel>
          </Border>
        </StackPanel>
      </Border>
</UserControl>
